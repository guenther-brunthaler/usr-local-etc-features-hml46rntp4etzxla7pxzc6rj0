# Version 2022.19
#
# $ ifup usb0=dhcp
#
# The above command is an example how use this configuration in order to
# configure existing (but yet down) interface "usb0" via DHCP. For instance,
# this might be used for connecting to a cell phone through a USB-cable with
# USB-tethering enabled at the other end.

iface dhcp inet dhcp
