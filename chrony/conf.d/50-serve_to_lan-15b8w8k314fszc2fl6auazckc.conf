# Serve time via NTP to (W)LAN.
#
# v2026.107

# Allow all computers with IP adresses 10.x.x.x to access this NTP server.
allow 10.0.0.0/8
 
# Bind to all network interfaces, but only for IPv4.
bindaddress 0.0.0.0
