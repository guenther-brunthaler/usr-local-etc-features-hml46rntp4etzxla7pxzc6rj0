This directory is NOT a template for /etc, but rather for /boot!

It should possibly have better been put into a separate repository,
but the number of configuration files in /boot is so small that it
did not seem to be worth the effort.
